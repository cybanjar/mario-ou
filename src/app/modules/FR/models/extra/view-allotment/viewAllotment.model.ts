export interface ReqViewAllotment {
  pvILanguage: number;
  fromName: string;
  toName: string;
  fromDate: string;
  toDate: string;
  resFlag: boolean;
  gFlag: boolean;
  cFlag: boolean;
  detailFlag: boolean;
  currRmType: string;
}

export interface ViewAllotment {
  STR: string;
}
